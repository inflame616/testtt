pipeline {
    agent any 
    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                // Thêm các lệnh build ở đây
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                // Thêm các lệnh test ở đây
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying..'
                // Thêm các lệnh deploy ở đây
            }
        }
    }
}
